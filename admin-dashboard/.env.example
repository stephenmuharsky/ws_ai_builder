# ── Airtable (primary data source) ──────────────────────────────────
# Create a Personal Access Token at https://airtable.com/create/tokens
# Scope: data.records:read on your base (read-only is sufficient for dashboard reads)
VITE_AIRTABLE_PAT=pat_your_token_here
VITE_AIRTABLE_BASE_ID=app_your_base_id_here

# Table names (must match your Airtable base exactly)
VITE_AIRTABLE_LEADS_TABLE=Leads
VITE_AIRTABLE_ADVISORS_TABLE=Advisor Info

# ── n8n (write operations — triggers downstream workflows) ──────────
# Actions like approve/reject/override still go through n8n because they
# trigger email outreach, AI enrichment, calendar booking, etc.
VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook

# ── Firm branding ───────────────────────────────────────────────────
VITE_FIRM_NAME=NorthStar Wealth Advisory
